<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="1dp"
        android:layout_centerInParent="true"
        android:id="@+id/recycler"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textColor="@color/black"
        android:text="today's tips are not posted yet. Please try later."
        android:visibility="gone"
        android:id="@+id/not_posted"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textColor="@color/black"
        android:text="Your account is not verified yet.\nPlease press settings icon at top right corner and send a request."
        android:visibility="gone"
        android:layout_marginHorizontal="25dp"
        android:gravity="center"
        android:id="@+id/not_verified"/>
</RelativeLayout>